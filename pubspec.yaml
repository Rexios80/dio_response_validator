name: dio_response_validator
description: An extension on Dio response futures to perform validation and data transformation
version: 0.3.0
homepage: https://github.com/Rexios80/dio_response_validator

environment:
  sdk: ^3.0.0

dependencies:
  dio: ^5.2.0

dev_dependencies:
  rexios_lints: ^15.0.1
  test: ^1.16.0
